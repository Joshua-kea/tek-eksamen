DOCKER
======

docker ps
docker ps -a
docker start tek-demo
docker stop tek-demo
docker rm tek-demo

docker build -t tek-image .
docker run -it ubuntu:22.04 bash
docker run -d -p 8080:80 tek-image

docker images
docker rmi tek-image
docker image prune
docker system prune

docker compose up
docker compose up -d
docker compose down
docker compose logs
